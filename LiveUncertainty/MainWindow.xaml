<Window x:Class="LiveUncertainty.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiveUncertainty"
        mc:Ignorable="d"
        Title="LUC" Height="459" Width="620">
    <Grid>
        <DockPanel VerticalAlignment="Top" HorizontalAlignment="Stretch" Grid.Row="1" Grid.ColumnSpan="3">
            <Menu DockPanel.Dock="Top" Height="20">
                <MenuItem Header="_File">
                    <MenuItem Header="_Load Profile"></MenuItem>
                    <MenuItem Header="_Save current Profile"></MenuItem>
                    <MenuItem Header="_Create New Profile" Name="create_profile" Click="CreateProfile_click"></MenuItem>
                    <MenuItem Header="_Settings"></MenuItem>
                    <MenuItem Header="_Exit"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Edit">
                    <MenuItem Header="_Edit Current Profile"></MenuItem>
                    <MenuItem Header="_Delete Current Profile"></MenuItem>
                    <MenuItem Header="_Manage all profiles"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Help">
                    <MenuItem Header="_Quickstart"></MenuItem>
                    <MenuItem Header="_Terminology Guide"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Tools">
                    <MenuItem Header="_Change Layout"></MenuItem>
                </MenuItem>

            </Menu>
        </DockPanel>
        <!-- Graph -->
        <lvc:CartesianChart Series="{Binding Collection}" LegendLocation="Right" Grid.ColumnSpan="3" Margin="15,50,15,10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1">
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Relative Expanded Uncertainty"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Flow Rate (m/s)"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>

        <!-- Labels that will be used for Displaying Data -->

        <Label Grid.Row="2" FontSize="14" FontWeight="Medium" FontFamily="Verdana" Margin="10,10,10,67" HorizontalAlignment="Center" VerticalAlignment="Stretch">Meter Info</Label>
        <Label Grid.Row="2" Grid.Column="2" FontSize="14" FontWeight="Medium" FontFamily="Verdana" Margin="10,10,10,67" HorizontalAlignment="Center" VerticalAlignment="Stretch">Operating Conditions</Label>
        <Label Grid.Row="2" Grid.Column="1" FontSize="14" FontWeight="Medium" FontFamily="Verdana" Margin="24,10,10,67" HorizontalAlignment="Center" VerticalAlignment="Stretch">Uncertainty Values</Label>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="184*"></RowDefinition>
            <RowDefinition Height="113*"></RowDefinition>
            <RowDefinition Height="72*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
    </Grid>
</Window>
