<Window x:Class="LiveUncertainty.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiveUncertainty"
        mc:Ignorable="d"
        Title="LUC" Height="400" Width="620">
    <Grid Margin="0,0,0,330" Height="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <DockPanel Grid.ColumnSpan="2">
            <Menu DockPanel.Dock="Top" Height="39">
                <MenuItem Header="_File">
                    <MenuItem Header="_Load Profile"></MenuItem>
                    <MenuItem Header="_Save current Profile"></MenuItem>
                    <MenuItem Header="_Create New Profile" Name="create_profile" Click="CreateProfile_click"></MenuItem>
                    <MenuItem Header="_Settings"></MenuItem>
                    <MenuItem Header="_Exit"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Edit">
                    <MenuItem Header="_Edit Current Profile"></MenuItem>
                    <MenuItem Header="_Delete Current Profile"></MenuItem>
                    <MenuItem Header="_Manage all profiles"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Help">
                    <MenuItem Header="_Quickstart"></MenuItem>
                    <MenuItem Header="_Terminology Guide"></MenuItem>
                </MenuItem>
                <MenuItem Header="_Tools">
                    <MenuItem Header="_Change Layout"></MenuItem>
                </MenuItem>

            </Menu>
        </DockPanel>
        <Grid>
            <lvc:CartesianChart Series="{Binding Collection}" LegendLocation="Right" Margin="1,41,3,-178" Grid.Row="1" VerticalAlignment="Stretch">
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis Title="Relative Expanded Uncertainty"></lvc:Axis>
                </lvc:CartesianChart.AxisY>
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Title="Flow Rate (m/s)"></lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="-1.169"/>
                        <RotateTransform/>
                        <TranslateTransform X="-0.98"/>
                    </TransformGroup>
                </lvc:CartesianChart.RenderTransform>
            </lvc:CartesianChart>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
        </Grid>

        <Grid MaxHeight="1000" Width="Auto">
            <Label x:Name="labelInfo" Content="Meter Information&#xD;&#xA;" Grid.ColumnSpan="2" Margin="20,229,198,-234"/>
            <Label x:Name="labelOpConditions" Content="Operating Conditions&#xA;" Grid.ColumnSpan="2" Margin="20,229,193,-234" Grid.Column="1"/>
            <Label x:Name="labelUncertianty" Content="Uncertainty Readings" Margin="20,229,2,-234" Grid.Column="2"/>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="20" MinHeight="20"/>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

    </Grid>
</Window>
